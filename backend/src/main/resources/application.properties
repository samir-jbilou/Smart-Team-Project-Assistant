spring.application.name=backend
# Connexion à la base de données PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/smart_team_db
spring.datasource.username=postgres
spring.datasource.password=S1a2m3i4r5

# Configuration JPA / Hibernate
# 'update' crée les tables automatiquement sans supprimer tes données existantes
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Désactiver l'icône de chargement Spring (optionnel, pour une console plus propre)
spring.main.banner-mode=CONSOLE

# Configuration JWT (Clé secrète et expiration en millisecondes - ici 24h)
app.jwt.secret=S1a2m3i4r5MaCleSecreteSuperSecuriseePourMonPFASamir2026
app.jwt.expiration=86400000